import styled from "@emotion/styled";
import { Badge } from "@mui/material";

export const FriendListItemBadge = styled(Badge)`
  .MuiBadge-badge {
    width: 12px;
    height: 12px;
    border-radius: 50%;
  }
`;
